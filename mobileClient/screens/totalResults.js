import React from "react";
import { View } from "react-native";
import Button from "../components/primaryButton.js";

export default function totalResults({ navigation }) {
  return <View style={{ flex: 1, justifyContent: "center" }}></View>;
}
